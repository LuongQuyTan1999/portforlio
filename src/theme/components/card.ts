import type { ComponentStyleConfig } from '@chakra-ui/theme';

const Card: ComponentStyleConfig = {
  baseStyle: {
    bg: 'blackAlpha.450',
    borderRadius: '15px',
    p: '1.5rem',
  },
};

export default Card;
